# GramGPT API — зависимости
# Установка: pip install -r requirements.txt

# FastAPI
fastapi==0.111.0
uvicorn[standard]==0.29.0

# База данных
sqlalchemy==2.0.30
asyncpg==0.29.0
alembic==1.13.1

# Авторизация
python-jose[cryptography]==3.3.0
bcrypt==4.0.1

# Pydantic — версия с готовыми wheels для Windows
pydantic==2.7.1
pydantic[email]==2.7.1
python-dotenv==1.0.1

# HTTP
httpx==0.27.0

# Celery + Redis
celery==5.3.6
redis==5.0.8
# ВАЖНО: для параллельных тасков использовать -P threads (не gevent!).
# gevent несовместим с asyncio: "Cannot run the event loop while another loop is running".
# threads встроены в Python, дополнительные зависимости не нужны.
# Запуск: celery worker -P threads -c 40

# Flower — web-UI для Celery (порт 5555 по умолчанию).
# Запуск: celery -A celery_app flower --port=5555
# Видно: очереди, активные таски, fail rate, история выполнения.
flower==2.0.1

aiohttp==3.9.5